<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .container2 {
            background-color: #F3E8EB;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 900px;
        }

        .content {
            font-size: 20px;
            border: 1px solid #F3E8EB;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>
<body>
    <div class="container2">
        <h1><%= post.title %></h1>
        <p>By <%= post.author?.username || "Anonymous" %> | <%= new Date(post.createdAt).toLocaleDateString() %></p>
        <img src="<%= post.images && post.images.length > 0 ? post.images[0] : '/default-image.jpg' %>" class="card-img-top" alt="Post Image" style="max-width: 100%;">
        <p class="content"><%= post.content %></p>
        <a href="/">홈으로 돌아가기</a>
    </div>
</body>
</html>
